// Wait until the HTML document is fully loaded before executing the scrip
document.addEventListener("DOMContentLoaded", function() {
    // Log a message to the console to indicate that the Secured Audio Player has loaded
    console.log("Secured Audio Player Loaded");
});
